/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      // remotePatterns: [
      //   {
      //     protocol: 'https',
      //     hostname: 'images.unsplash.com', // This is the actual image CDN domain Unsplash uses
      //   },
      // ],
    },
  };
  
  export default nextConfig;
  